{
  "version": 2,
  "framework": null,
  "outputDirectory": "./",
  "builds": [
    {
      "src": "index.html",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "npm install",
        "buildCommand": "echo 'Serving static HTML'",
        "outputDirectory": "./"
      }
    }
  ],
  "routes": [
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
